import React from 'react';
import AppointmentPoster from '../AppointmentPoster/AppointmentPoster';

const Appointment = () => {
    return (
        <div>
            <AppointmentPoster></AppointmentPoster>
        </div>
    );
};

export default Appointment;